export async function comprimirFotoBasica(archivo) {
  // Paso 36: dejamos la foto original controlada/validada.
  // La compresión real queda separada para no romper compatibilidad en celulares.
  return archivo;
}